import React from 'react';
import styled from 'styled-components';
import {Container} from "./Logos";

const GallaryItems = styled.div`
`;
const Gallary = () => {
    return (
            <Container style={{backgroundColor: 'red'
            }}>
                <h2>Feautured</h2>
            </Container>
    );
};

export default Gallary;